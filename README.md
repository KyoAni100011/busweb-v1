System & Infrastructure
[X] Configure ESLint, Prettier, Husky for developing tools

Setup:

- npm install

Run:

- npm run de

Note:

- Before commit, husky has been set up in .husky include lint, format and run test. If you commit failed check your code to fix format and follow the rule eslint and commit again
